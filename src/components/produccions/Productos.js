import React from 'react'
import { Card, CardHeader, } from '@material-ui/core';

export default function Productos(props){
    const {produccion, showMessage} = props
    const agregar = () => {

    }

    const eliminar = () => {
        
    }
    return (
        <Card>
            <CardHeader title="Productos"
                
                >
            </CardHeader>

        </Card>
    )
}